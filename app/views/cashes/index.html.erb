<p id="notice"><%= notice %></p>

<h1>Efectivo</h1>

<table>
  <thead>
    <tr>
      <th>Concepto</th>
      <th>Moeda</th>
      <th>Ingreso</th>
      <th>Egreso</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cashes.each do |cash| %>
      <tr>
        <td><%= cash.concept %></td>
        <td><%= cash.coin %></td>
        <td><%= cash.entry %></td>
        <td><%= cash.egress %></td>
        <td><%= link_to 'Show', cash %></td>
        <td><%= link_to 'Edit', edit_cash_path(cash) %></td>
        <td><%= link_to 'Destroy', cash, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cash', new_cash_path %>
